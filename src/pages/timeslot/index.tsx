import Layout from "../layout.tsx/app";

const IndexTimeSlot = () => {

    return (
        <Layout
            withPageTitle
            title="Time Slot Management"
           
        >
            <div className="w-full bg-white rounded-lg">
               time slot management
            </div>
        </Layout>
    );
};

export default IndexTimeSlot;